# for direnv
export DOCKER_REGISTRY="ghcr.io/jhee-group"
export DOCKER_BUILDKIT=1

export JHEEP_PATH="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
export PATH=${PATH}:"${JHEEP_PATH}/script"
